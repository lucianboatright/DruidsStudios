import styled from 'styled-components';

export const Grid = styled.div({
  gap: '1rem',
  width: '100%',
  margin: '2rem 0',
});

export const Image = styled.img( {
  maxWidth: '40%',
  marginRight: '0.5rem',
  height: 'auto',
  borderRadius: '8px',
  marginBottom: '1rem',
});